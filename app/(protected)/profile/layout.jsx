export const metadata = {
  title: "My Profile | HatBari",
  description:
    "View your orders, manage addresses, and update account settings.",
};

export default function ProfileLayout({ children }) {
  return children;
}
